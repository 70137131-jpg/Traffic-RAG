# Direct dependencies, pinned for reproducible builds.
# For a fully reproducible environment (incl. transitive deps) install from
# requirements.lock instead:  pip install -r requirements.lock

# Web serving
flask==3.1.3
gunicorn==26.0.0

# Config / env
python-dotenv==1.2.2

# RAG pipeline (LangChain 1.x — migrated off the sunset langchain-community paths)
langchain-core==1.4.9
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langchain-google-genai==4.2.7

# Storage / retrieval
psycopg[binary]==3.3.4
psycopg_pool==3.3.1
pgvector==0.5.0
numpy==2.5.1

# Models
sentence-transformers==5.6.0

# Reliability (timeouts/retries on the LLM call)
tenacity==9.1.4
